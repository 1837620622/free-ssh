# 爪云 (Claw Cloud) 部署 SSH 服务

> Claw Cloud Run 是一款简洁高效的无服务器部署平台，支持一键部署 Docker 容器。

## 平台信息

| 项目 | 说明 |
|------|------|
| 官网 | https://console.run.claw.cloud |
| 文档 | https://docs.run.claw.cloud |
| 镜像 | `ghcr.io/vevc/ubuntu:25.7.14` |
| 端口 | 22 |
| 难度 | ⭐ 简单 |

## 免费额度

- 新注册送 **$5**，有效期一个月
- GitHub 账户注册满 180 天，每月额外 **$5**

## 部署步骤

### 1. 注册账号

访问 [Claw Cloud 控制台](https://console.run.claw.cloud)，使用 GitHub 账号登录。

> 💡 建议使用注册满 180 天的 GitHub 账号，可获得每月 $5 免费额度

### 2. 创建应用

1. 登录控制台后，点击 **APP Launchpad**
2. 点击 **Create App** 或 **New Deployment**
3. 选择 **Deploy from Image**

### 3. 配置镜像

填写以下信息：

| 配置项 | 值 |
|--------|-----|
| Image | `ghcr.io/vevc/ubuntu:25.7.14` |
| Port | `22` |
| Protocol | `TCP` |

### 4. 配置环境变量

添加环境变量：

| 变量名 | 值 |
|--------|-----|
| ROOT_PASSWORD | 你的密码 |

### 5. 配置启动命令

在 **Command** 或 **Entrypoint** 中填入：

```bash
/bin/bash -c "echo root:$ROOT_PASSWORD | chpasswd; sed -i 's/#PermitRootLogin prohibit-password/PermitRootLogin yes/' /etc/ssh/sshd_config; sed -i 's/PermitRootLogin prohibit-password/PermitRootLogin yes/' /etc/ssh/sshd_config; mkdir -p /var/run/sshd; /usr/sbin/sshd -D"
```

### 6. 选择区域

可选区域：
- Asia（亚洲）
- US（美国）
- EU（欧洲）

推荐选择 **Asia** 获得最低延迟。

### 7. 部署

点击 **Deploy** 按钮，等待部署完成（约 30 秒）。

### 8. 获取访问地址

部署完成后，在 **Networking** 或 **Domains** 部分：

1. 查看分配的域名和端口
2. 如果是 TCP 服务，记录 TCP 代理地址

### 9. 连接 SSH

```bash
ssh root@<分配的地址> -p <分配的端口>
```

## 使用私有镜像仓库

Claw Cloud 提供免费私有镜像仓库：

```bash
# 构建镜像
docker build -t registry.clawcloud.io/your-username/ssh-server:latest .

# 推送镜像
docker push registry.clawcloud.io/your-username/ssh-server:latest
```

然后在部署时使用该镜像地址。

## 特点

- ✅ 操作简单，纯网页配置
- ✅ GitHub 账号免费额度
- ✅ 私有镜像仓库
- ✅ 自动 HTTPS
- ✅ 实时日志查看
- ✅ 多区域部署

## 控制台功能

- **实时日志**：查看 stdout/stderr
- **资源监控**：CPU 和内存使用情况
- **版本管理**：回滚到历史版本
- **自定义域名**：绑定自己的域名

## 注意事项

1. SSH 服务需要使用 **TCP** 协议
2. 确保启动命令中 `-D` 参数不能省略
3. 免费额度有限，注意用量
4. GitHub 账号需注册满 180 天才能获得每月免费额度
